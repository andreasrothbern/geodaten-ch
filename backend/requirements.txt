# Geodaten Schweiz API - Backend Dependencies
# ============================================

# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0

# HTTP Client (async)
httpx==0.26.0

# Validation
pydantic==2.5.3

# CORS & Middleware
python-multipart==0.0.6

# Environment
python-dotenv==1.0.0

# Für Produktion
gunicorn==21.2.0

# swissBUILDINGS3D GDB Parsing (On-Demand Height Fetch)
geopandas>=0.14.0
fiona>=1.9.0

# Document Generation (Word)
python-docx>=1.1.0

# Image handling
Pillow>=10.0.0

# SVG to PNG - nicht verfügbar ohne Cairo
# cairosvg, svglib, pycairo brauchen alle C-Compiler + Cairo
# Wir verwenden stattdessen Pillow für direkte PNG-Generierung

# Claude API für SVG-Generierung
anthropic>=0.39.0
